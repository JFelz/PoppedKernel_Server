# Django Settings
SECRET_KEY=your-insecure-secret-key-for-development
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost,.ngrok.io

# Database Settings (PostgreSQL)
DB_NAME=ecommerce_db
DB_USER=ecommerce_user
DB_PASSWORD=ecommerce_password
DB_HOST=db
DB_PORT=5432

# Google OAuth Settings (Required for django-allauth)
# NOTE: These must be set in the Django Admin Site after initial setup.
# These variables are primarily for local development and testing.
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_SECRET=your_google_secret
